export const httpsReg = /^https:\/\//;
export const numberReg = /^[0-9]+.?[0-9]*$/;
export const objStrReg = /^\{(.*)\}$/;
export const arrayStrReg = /^\[(.*)\]$/;
export const doubleArrayStrReg = /^\[\[(.*)\]\]$/;
